const Packets = require("../Packets");
const {createPacket, sendPacket} = require("../PacketUtilities");
const ExtendedPacket = require('../BufferExtend')

module.exports = (player, packet) => {
	debugger
};
